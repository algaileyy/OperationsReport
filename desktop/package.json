{
  "name": "operations-report-desktop",
  "productName": "Operations Report",
  "version": "1.0.0",
  "private": true,
  "description": "Desktop app for entering monthly team numbers into the Operations Report.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . \"Operations Report\" --platform=win32 --arch=x64 --out=dist --overwrite --asar --icon=icon.ico"
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "electron-packager": "^17.1.2"
  }
}
